# Copie este arquivo para ".env" (que já está no .gitignore) e preencha com
# os valores reais. Usado só pelo scripts/registerCommands.ts (rodado local,
# fora da Edge Function).
DISCORD_TOKEN=
DISCORD_APPLICATION_ID=1534197949758963732
